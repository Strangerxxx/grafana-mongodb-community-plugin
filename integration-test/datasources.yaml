apiVersion: 1
datasources:
- name: mongodb
  type: mongodb-community-datasource
  access: proxy
  jsonData:
    url: mongodb://mongodb:27017
  secureJsonData:
    username: root
    password: rootPassword

